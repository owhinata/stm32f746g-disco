# Documentation site (mkdocs Material + i18n).
mkdocs>=1.6
mkdocs-material>=9.5
mkdocs-static-i18n>=1.2

# TFLM backend build (CONFIG_NN_BACKEND=tflm, issue #86): the tflite-micro Makefile
# is evaluated by create_tflm_tree.py at CMake configure time, and its example
# generate_cc_arrays.py step imports these.  Only needed when building the tflm
# variant; the default (null/stedgeai) firmware never uses them.
numpy
Pillow
